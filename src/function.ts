// ************************************************************ //

function makeChai(type: string, cups: number) {
  console.log(`this ${type} of chai, and ${cups}`)
}

makeChai('masala', 2)

// ****************************************************************** */

function abc (type?: string) {}   // optional
function abc2 (type: string = 'Bangla') {}   // default value

/********************************************************************* */

function chai(value: number): number {   // function number return করবে
  return value
}

/********************************************************************* */